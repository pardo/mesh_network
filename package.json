{
  "version": "1.0.0",
  "scripts": {
    "build": "rm -R docs && mkdir docs && parcel build --dist-dir docs --public-url https://pardo.github.io/mesh_network/ index.html",
    "serve": "parcel serve index.html",
    "test": "standard --fix *.js",
    "ncu": "ncu"
  },
  "dependencies": {
    "firebase": "^9.8.0",
    "simple-peer": "^9.11.1"
  },
  "devDependencies": {
    "events": "^3.3.0",
    "npm-check-updates": "^16.7.4",
    "parcel": "^2.0.0",
    "process": "^0.11.10",
    "standard": "^14.3.4"
  },
  "engines": {
    "node": "16.14.2"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
